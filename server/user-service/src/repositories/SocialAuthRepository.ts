class SocialAuthRepository {
	static async init(provider: any, userData: any) {
		// Logica para iniciar a autenticação com a rede social
		// Exemplo: Chamar uma API externa do provedor
	}

	static async callback(provider: any, callbackData: any) {
		// Logica para tratar o callback de autenticação da rede social
		// Exemplo: Chamar uma API externa do provedor
	}
}

export { SocialAuthRepository };
